<h1>Edit {{que.name}}</h1>

<form action="/user/{{userId}}/kitchen/{{kitchenId}}/que/{{que._id}}?_method=PUT" method="POST">
  <div>
    <label for="name">Que Name: </label>
    <input type="text" name="name" value="{{que.name}}" />
  </div>
  <div>
    <label for="restaurant">Restaurant:</label>
    <input type="text" name="restaurant" value="{{que.restaurant}}" />
  </div>
  <button type="submit">Update Que</button>
</form>

<a href="/user/{{userId}}/kitchen/{{kitchenId}}/que/{{queId}}">return</a>